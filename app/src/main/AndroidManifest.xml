<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Book2act"></activity>
        <activity android:name=".Book1act" />
        <activity android:name=".FutuAct" />
        <activity android:name=".MyBooksAct" />
        <activity android:name=".CatAct" />
        <activity android:name=".SugsAct" />
        <activity android:name=".AddbookRead" />
        <activity android:name=".ReadingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>